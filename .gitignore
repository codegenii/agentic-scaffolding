# Claude Code local permission grants — per-checkout, not shared.
.claude/settings.local.json

# Worktrees created by the feature/chore/resume commands.
.claude/worktrees/

# Python
__pycache__/

# run-journal db — machine-level by default; never commit a journal
runs.db
runs.db-wal
runs.db-shm
