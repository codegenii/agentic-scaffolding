# Architecture

This document is the index to the project's per-subsystem architecture detail. For *why* particular technologies were chosen, see [decisions.md](decisions.md).

<!-- /init-project seeds the summary. Grow this as the project takes shape.
Keep it an INDEX: a short "what this is" plus pointers to per-subsystem pages
under docs/architecture/. Agents are told to read the index plus only the one
subsystem page their task touches — so keep each page focused. -->

## What this is

`${PROJECT_NAME}` — `${TAGLINE}`.

<one-paragraph overview of the main subsystems and how they fit together>

## Subsystems

<!-- As subsystems appear, add a pointer per line:
- [Subsystem name](architecture/<name>.md) — one-line description.
Read only the subsystem(s) relevant to the work at hand. -->

## Guardrails

<!-- Cross-cutting rules the implementation must hold. The load-bearing ones
also belong in .claude/agents/conventions/invariants.md so reviewers enforce them. -->
